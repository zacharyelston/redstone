# Redstone Environment Configuration
# Following the "Built for Clarity" design philosophy
# Comprehensive project management and monitoring stack
# Auto-provisioning ready - all defaults ensure working deployment

# === DEPLOYMENT CONFIGURATION ===
ENVIRONMENT=development
CLUSTER_NAME=docker-local
LOG_LEVEL=info

# === SERVICE VERSIONS ===
# Core Services
POSTGRES_VERSION=15-alpine
REDIS_VERSION=7-alpine
REDMICA_VERSION=3.1.7
LDAP_VERSION=1.5.0

# Monitoring Stack
GRAFANA_VERSION=10.1.0
PROMETHEUS_VERSION=v2.47.0
LOKI_VERSION=2.9.0
PROMTAIL_VERSION=2.9.0
FLUENT_BIT_VERSION=2.1.8

# Exporters
NODE_EXPORTER_VERSION=v1.6.1
POSTGRES_EXPORTER_VERSION=v0.13.2
REDIS_EXPORTER_VERSION=v1.54.0
BLACKBOX_EXPORTER_VERSION=v0.24.0
CADVISOR_VERSION=v0.47.2

# === DATABASE CONFIGURATION ===
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
POSTGRES_DB=redmica_production

# === REDIS CONFIGURATION ===
REDIS_PASSWORD=redis

# === REDMICA CONFIGURATION ===
REDMICA_SECRET_KEY_BASE=changemeinproduction
REDMICA_PORT=3000

# === LDAP CONFIGURATION ===
LDAP_DOMAIN=redstone.local
LDAP_ADMIN_PASSWORD=admin
LDAP_CONFIG_PASSWORD=config
LDAP_BASE_DN=dc=redstone,dc=local
LDAP_PORT=389
LDAPS_PORT=636

# === MONITORING PORTS ===
# Visualization & Monitoring
GRAFANA_PORT=3001
PROMETHEUS_PORT=9090
LOKI_PORT=3100

# Exporters
NODE_EXPORTER_PORT=9100
POSTGRES_EXPORTER_PORT=9187
REDIS_EXPORTER_PORT=9121
BLACKBOX_EXPORTER_PORT=9115
CADVISOR_PORT=8080

# === MONITORING CONFIGURATION ===
GRAFANA_ADMIN_PASSWORD=admin

# === RELEASE.COM CONFIGURATION ===
RELEASE_API_TOKEN=your_release_api_token_here
RELEASE_PROJECT_NAME=redstone
RELEASE_ENVIRONMENT=development
RELEASE_APP_ID=your_app_id_here
RELEASE_ENV_ID=your_env_id_here

# === SECURITY SETTINGS ===
JWT_SECRET=replace_with_secure_random_string
SESSION_SECRET=replace_with_secure_random_string

# === NOTES FOR AUTO-PROVISIONING ===
# 1. All ports are configured to avoid conflicts
# 2. Redmica runs on 3000, Grafana on 3001 (not 3002 as shown in some places)
# 3. LDAP uses non-standard ports (389->1389) to avoid conflicts
# 4. All passwords should be generated securely for production
# 5. CLUSTER_NAME should be unique per customer deployment
# 6. ENVIRONMENT should be set to 'production' for customer deployments